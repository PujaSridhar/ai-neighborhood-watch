{
    "name": "ai-neighborhood-watch-root",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build",
        "dev": "echo 'Run api and frontend manually or install concurrently'"
    },
    "engines": {
        "node": "18.x"
    }
}